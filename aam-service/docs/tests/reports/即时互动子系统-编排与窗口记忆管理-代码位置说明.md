# 即时互动子系统 - 编排与窗口记忆管理代码位置说明

**创建日期**: 2025-11-13  
**版本**: v1.0

---

## 📍 代码位置概览

### 1. Pipeline 编排（当前实现状态：40%）

#### 当前实现位置

**1.1 MCP 丰富化端点（部分实现）**

```35:125:aam-service/src/api/controllers/mcp_controller.py
class SessionContext(BaseModel):
    """會話上下文模型"""
    session_id: str = Field(..., description="會話 ID")
    current_query: str = Field(..., description="當前查詢")
    short_term_memory: List[Message] = Field(
        default_factory=list, description="短期記憶（對話歷史）"
    )
```

**文件**: `src/api/controllers/mcp_controller.py`
- **功能**: 接收 MCP 丰富化请求
- **职责**: HTTP 请求/响应处理
- **缺失**: 不是完整的 Pipeline 编排器

**1.2 记忆服务 enrich 方法（部分实现）**

```54:124:aam-service/src/core/services/memory_service.py
    async def enrich(self, mcp: PartialMCP) -> EnrichedMCP:
        """
        豐富化 MCP（同步 API 調用）
        
        Args:
            mcp: 部分 MCP（請求體）
            
        Returns:
            豐富化後的 MCP（響應體）
        """
        user_id = mcp.user_profile.user_id
        query = mcp.session_context.current_query
        session_id = mcp.session_context.session_id
        
        logger.info(
            f"Enriching MCP for user_id={user_id}, session_id={session_id}, query={query[:50]}..."
        )
        
        try:
            # 並行查詢知識庫和用戶畫像
            knowledge_docs, user_profile = await asyncio.gather(
                self.knowledge_store.search(query, user_id),
                self.persona_store.get(user_id),
                return_exceptions=True,
            )
            
            # 處理知識庫查詢結果
            if isinstance(knowledge_docs, Exception):
                logger.error(
                    f"Failed to search knowledge store: {knowledge_docs}",
                    exc_info=knowledge_docs,
                )
                knowledge_docs = []
            
            # 處理用戶畫像查詢結果
            if isinstance(user_profile, Exception):
                logger.error(
                    f"Failed to get user profile: {user_profile}",
                    exc_info=user_profile,
                )
                user_profile = None
            
            # 轉換用戶畫像為豐富化版本
            enriched_profile = self._convert_user_profile_to_enriched(
                user_profile, user_id
            )
            
            # 組裝檢索到的知識（目前 kg_triples 為空，因為接口不支持返回三元組）
            retrieved_knowledge = RetrievedKnowledge(
                docs=knowledge_docs,
                kg_triples=[],  # 當前接口不支持返回三元組
            )
            
            # 創建元數據
            metadata = Metadata()
            
            # 組裝並返回 EnrichedMCP
            enriched_mcp = EnrichedMCP(
                metadata=metadata,
                user_profile=enriched_profile,
                session_context=mcp.session_context,
                retrieved_knowledge=retrieved_knowledge,
            )
            
            logger.info(
                f"Successfully enriched MCP for user_id={user_id}, "
                f"found {len(knowledge_docs)} docs, "
                f"request_id={metadata.request_id}"
            )
            
            return enriched_mcp
```

**文件**: `src/core/services/memory_service.py`
- **功能**: 执行 MCP 丰富化（RAG 检索）
- **职责**: 从知识库和用户画像中检索相关信息
- **缺失**: 
  - ❌ 不管理短期记忆
  - ❌ 不调用 LLM
  - ❌ 不返回最终响应给用户
  - ❌ 不是完整的 Pipeline 编排器

#### 架构要求的完整 Pipeline 流程

根据架构图，完整的 Pipeline 应该执行以下步骤：

```
1. 接收用戶查詢
   ↓
2. 管理短期記憶（窗口記憶）
   ↓
3. 發起 MCP 調用以豐富上下文（RAG 檢索）
   ↓
4. 將最終的 Prompt 提交給 LLM
   ↓
5. 將結果返回給用戶
```

**当前缺失的部分**:
- ❌ **步骤 1**: 没有统一的入口接收用户查询
- ⚠️ **步骤 2**: 短期记忆管理不完整（见下方窗口记忆管理）
- ✅ **步骤 3**: MCP 丰富化已实现（`memory_service.enrich()`）
- ❌ **步骤 4**: 没有调用 LLM 的逻辑
- ❌ **步骤 5**: 没有返回响应给用户的逻辑

**建议实现位置**:
- 新建文件: `src/core/services/pipeline_orchestrator.py`
- 或者: 在 `src/api/controllers/` 中创建完整的对话控制器

---

### 2. 窗口记忆管理（当前实现状态：30%）

#### 当前实现位置

**2.1 SessionContext 模型（部分实现）**

```35:41:aam-service/src/models/api/mcp.py
class SessionContext(BaseModel):
    """會話上下文模型"""
    session_id: str = Field(..., description="會話 ID")
    current_query: str = Field(..., description="當前查詢")
    short_term_memory: List[Message] = Field(
        default_factory=list, description="短期記憶（對話歷史）"
    )
```

**文件**: `src/models/api/mcp.py`
- **功能**: 定义短期记忆的数据结构
- **职责**: 存储对话历史（`short_term_memory` 字段）
- **缺失**:
  - ❌ 没有窗口大小限制
  - ❌ 没有滑动窗口机制
  - ❌ 没有上下文窗口管理逻辑

#### 架构要求的窗口记忆管理

根据架构图，窗口记忆应该：

1. **基于窗口的记忆 (Memory by Window)**
   - 利用主要 LLM 的原生上下文视窗
   - 维持对话的连贯性
   - 高效且无状态的短期记忆实现

2. **窗口大小管理**
   - 可配置的上下文窗口大小
   - 当对话历史超过窗口大小时，自动截断或压缩
   - 保留最重要的对话内容

3. **滑动窗口机制**
   - 新对话加入时，自动移除最旧的对话
   - 保持窗口大小在限制范围内

**当前缺失的功能**:
- ❌ 窗口大小配置
- ❌ 窗口截断逻辑
- ❌ 对话重要性评估（用于决定保留哪些对话）
- ❌ 滑动窗口实现

**建议实现位置**:
- 新建文件: `src/core/services/window_memory_manager.py`
- 或者: 在 `MemoryService` 中添加窗口记忆管理方法

---

## 🔍 具体代码位置总结

### Pipeline 编排相关代码

| 功能 | 文件位置 | 实现状态 | 说明 |
|------|---------|---------|------|
| **MCP 丰富化端点** | `src/api/controllers/mcp_controller.py` | ✅ 已实现 | HTTP 端点，接收 MCP 请求 |
| **MCP 丰富化逻辑** | `src/core/services/memory_service.py` (enrich 方法) | ✅ 已实现 | RAG 检索逻辑 |
| **Pipeline 编排器** | ❌ **不存在** | ❌ 未实现 | 应该协调整个对话流程 |
| **LLM 调用** | ❌ **不存在** | ❌ 未实现 | 应该在 Pipeline 中调用 LLM |
| **响应返回** | ❌ **不存在** | ❌ 未实现 | 应该返回最终响应给用户 |

### 窗口记忆管理相关代码

| 功能 | 文件位置 | 实现状态 | 说明 |
|------|---------|---------|------|
| **短期记忆数据结构** | `src/models/api/mcp.py` (SessionContext) | ✅ 已实现 | 定义 `short_term_memory` 字段 |
| **窗口大小配置** | ❌ **不存在** | ❌ 未实现 | 应该可配置窗口大小 |
| **窗口截断逻辑** | ❌ **不存在** | ❌ 未实现 | 应该自动截断超出窗口的对话 |
| **滑动窗口机制** | ❌ **不存在** | ❌ 未实现 | 应该实现滑动窗口 |
| **窗口记忆管理器** | ❌ **不存在** | ❌ 未实现 | 应该统一管理窗口记忆 |

---

## 📝 需要实现的代码结构

### 1. Pipeline 编排器（建议实现）

**文件**: `src/core/services/pipeline_orchestrator.py`

```python
class PipelineOrchestrator:
    """
    Pipeline 编排器 - 协调整个对话流程
    
    职责：
    1. 接收用户查询
    2. 管理短期记忆（窗口记忆）
    3. 发起 MCP 调用以丰富上下文
    4. 将最终的 Prompt 提交给 LLM
    5. 将结果返回给用户
    """
    
    def __init__(
        self,
        memory_service: IMemoryService,
        llm_provider: IModelProvider,
        window_memory_manager: WindowMemoryManager,
    ):
        self.memory_service = memory_service
        self.llm_provider = llm_provider
        self.window_memory_manager = window_memory_manager
    
    async def process_query(
        self,
        user_id: str,
        session_id: str,
        query: str,
    ) -> str:
        """
        处理用户查询的完整流程
        
        1. 管理短期记忆（窗口记忆）
        2. 发起 MCP 调用以丰富上下文
        3. 调用 LLM 生成响应
        4. 更新短期记忆
        5. 返回响应
        """
        # TODO: 实现完整流程
        pass
```

### 2. 窗口记忆管理器（建议实现）

**文件**: `src/core/services/window_memory_manager.py`

```python
class WindowMemoryManager:
    """
    窗口记忆管理器 - 管理基于窗口的短期记忆
    
    职责：
    1. 管理对话历史窗口
    2. 自动截断超出窗口大小的对话
    3. 实现滑动窗口机制
    4. 保持对话连贯性
    """
    
    def __init__(
        self,
        max_window_size: int = 10,  # 最大窗口大小（对话轮数）
        max_tokens: int = 4096,      # 最大 token 数
    ):
        self.max_window_size = max_window_size
        self.max_tokens = max_tokens
    
    def add_message(
        self,
        session_id: str,
        message: Message,
    ) -> List[Message]:
        """
        添加新消息到窗口记忆
        
        如果超出窗口大小，自动移除最旧的对话
        """
        # TODO: 实现窗口管理逻辑
        pass
    
    def get_window_memory(
        self,
        session_id: str,
    ) -> List[Message]:
        """
        获取当前窗口记忆
        """
        # TODO: 实现获取逻辑
        pass
    
    def truncate_window(
        self,
        messages: List[Message],
    ) -> List[Message]:
        """
        截断窗口记忆，保留最重要的对话
        """
        # TODO: 实现截断逻辑
        pass
```

---

## 🎯 总结

### Pipeline 编排

**当前状态**: 功能分散，没有统一的编排器
- ✅ MCP 丰富化已实现（`memory_service.enrich()`）
- ❌ 缺少完整的 Pipeline 编排器
- ❌ 缺少 LLM 调用逻辑
- ❌ 缺少响应返回逻辑

**代码位置**:
- 部分实现: `src/api/controllers/mcp_controller.py`
- 部分实现: `src/core/services/memory_service.py` (enrich 方法)
- **缺失**: `src/core/services/pipeline_orchestrator.py`（需要新建）

### 窗口记忆管理

**当前状态**: 只有数据结构，没有管理逻辑
- ✅ 数据结构已定义（`SessionContext.short_term_memory`）
- ❌ 缺少窗口大小管理
- ❌ 缺少滑动窗口机制
- ❌ 缺少窗口截断逻辑

**代码位置**:
- 数据结构: `src/models/api/mcp.py` (SessionContext)
- **缺失**: `src/core/services/window_memory_manager.py`（需要新建）

---

**报告生成时间**: 2025-11-13

