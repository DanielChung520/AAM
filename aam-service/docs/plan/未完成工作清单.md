# AAM 系统未完成工作清单

**创建日期**: 2025-11-12  
**版本**: v1.0  
**基准文档**: 
- `AAM (AI-Augmented Memory) SA v1.md` - 系统架构文档
- `AAM Phase II.md` - Phase II 实施计划
- `整体进度核对报告.md` - 进度报告

---

## 📊 总体概览

根据 SA 文档、Phase II 计划和进度报告的比对，以下是**尚未完成的工作清单**：

| 类别 | 项目数 | 高优先级 | 中优先级 | 低优先级 |
|------|--------|---------|---------|---------|
| **核心功能** | 2 | 0 | 1 | 1 |
| **训练管道** | 3 | 1 | 1 | 1 |
| **性能优化** | 3 | 0 | 2 | 1 |
| **生产环境** | 3 | 0 | 3 | 0 |
| **部署策略** | 1 | 0 | 1 | 0 |
| **总计** | **12** | **1** | **8** | **3** |

---

## 🔴 高优先级（必须完成）

### 1. 实际训练验证 ⚠️

**来源**: `整体进度核对报告.md` - 下一步计划

**描述**: 
- 在有 GPU 的环境中执行实际 LoRA 训练
- 验证训练后的模型性能（NER > 80%, KT > 75%）
- 集成训练后的模型到批次四

**状态**: ❌ 未开始

**相关文件**:
- `src/training/train_lora.py` - 训练脚本已实现，但未在实际 GPU 环境验证
- `src/training/data_exporter.py` - 数据导出器已实现

**验收标准**:
- [ ] 在 GPU 环境中成功执行训练
- [ ] 训练后的模型性能达到目标（NER > 80%, KT > 75%）
- [ ] 训练后的模型可以正常集成到批次四

**预计工期**: 1-2 周（取决于 GPU 资源可用性）

---

## 🟡 中优先级（重要但可延后）

### 2. 预处理功能（Pre-processing）⚠️

**来源**: `AAM (AI-Augmented Memory) SA v1.md` - 步骤 2

**描述**: 
- 后端可選擇先調用 **Eb-MM**，對用戶問題進行快速的意圖分類或專有實體提取
- 這能幫助後續的記憶檢索更精準，同時節省成本
- 返回預處理結果（如意圖：`查詢庫存`，實體：`產品A`）

**状态**: ❌ 未实现

**注意**: 
- 此功能可能属于 SmartQ（上层应用）的职责，而非 AAM 服务
- 需要确认架构设计：是在 AAM 服务中提供 API，还是由 SmartQ 直接调用 Eb-MM？

**相关文件**:
- `src/infrastructure/ai/eb_mm_analysis_model.py` - Eb-MM 模型已实现，但未提供预处理 API
- `src/api/controllers/mcp_controller.py` - 可能需要添加预处理端点

**验收标准**:
- [ ] 提供预处理 API 端点（如 `POST /v1/preprocess`）
- [ ] 支持意图分类
- [ ] 支持专有实体提取
- [ ] 返回格式符合 MCP 协议

**预计工期**: 1 周

---

### 3. LoRA 训练 CLI 命令 ⚠️

**来源**: `AAM Phase II.md` - 批次五可选功能

**描述**: 
- 实现训练 CLI 命令，方便手动触发训练
- 支持命令行参数配置（数据路径、模型路径、训练参数等）

**状态**: ❌ 未实现

**相关文件**:
- `src/training/train_lora.py` - 训练脚本已实现，但缺少 CLI 接口
- 需要创建 `src/training/cli.py` 或使用 `click`/`argparse`

**验收标准**:
- [ ] 实现 CLI 命令（如 `python -m src.training.train_lora --data-path ...`）
- [ ] 支持所有训练参数配置
- [ ] 支持数据导出和训练一体化流程
- [ ] 包含帮助文档

**预计工期**: 3-5 天

---

### 4. LoRA 训练任务调度 ⚠️

**来源**: `AAM Phase II.md` - 批次五可选功能

**描述**: 
- 实现自动定期训练任务调度
- 使用 Celery/APScheduler 等任务调度框架
- 支持配置训练频率（如每周一次）

**状态**: ❌ 未实现

**相关文件**:
- `src/training/train_lora.py` - 训练脚本已实现
- 需要创建 `src/training/scheduler.py` 或集成 Celery

**验收标准**:
- [ ] 实现任务调度器
- [ ] 支持配置训练频率
- [ ] 支持任务状态监控
- [ ] 支持任务失败重试

**预计工期**: 1 周

---

### 5. 批处理支持（EB-mM）⚠️

**来源**: `AAM Phase II.md` - 批次四 Task 4.1.4

**描述**: 
- 实现批处理（batch processing）支持
- 提升处理效率，减少模型调用次数
- 目标：批处理效率提升 > 30%

**状态**: ❌ 未实现

**相关文件**:
- `src/infrastructure/ai/eb_mm_analysis_model.py` - 当前只支持单条处理
- `src/infrastructure/ai/unified_model_service.py` - 可能需要扩展支持批处理

**验收标准**:
- [ ] 实现批量知识提取接口
- [ ] 批处理效率提升 > 30%
- [ ] 支持配置批处理大小
- [ ] 包含性能测试

**预计工期**: 1 周

---

### 6. 缓存机制（EB-mM）⚠️

**来源**: `AAM Phase II.md` - 批次四 Task 4.1.4

**描述**: 
- 实现缓存机制（可选，使用 LRU 缓存）
- 缓存相同文本的分析结果，减少重复计算
- 提升响应速度

**状态**: ❌ 未实现

**相关文件**:
- `src/infrastructure/ai/eb_mm_analysis_model.py` - 需要添加缓存层
- 可以使用 `functools.lru_cache` 或 Redis

**验收标准**:
- [ ] 实现缓存机制（LRU 或 Redis）
- [ ] 支持缓存配置（TTL、大小等）
- [ ] 缓存命中率 > 50%（如果适用）
- [ ] 包含缓存失效策略

**预计工期**: 3-5 天

---

### 7. 性能监控 ⚠️

**来源**: `整体进度核对报告.md` - 下一步计划

**描述**: 
- 添加性能监控指标
- 监控各层级模型的响应时间、使用率、降级频率等
- 使用 Prometheus 指标

**状态**: ❌ 未实现

**相关文件**:
- 需要创建 `src/infrastructure/monitoring/` 目录
- 需要集成 Prometheus 客户端库

**验收标准**:
- [ ] 实现 Prometheus 指标收集
- [ ] 监控各层级模型响应时间
- [ ] 监控模型使用率和降级频率
- [ ] 提供 Grafana 仪表板配置

**预计工期**: 1 周

---

### 8. Kubernetes 配置 ⚠️

**来源**: `AAM Agent SD v2.md` - 7.0 基础设施与部署

**描述**: 
- 创建 Kubernetes 配置文件
- 每个组件（AAM Service, KAg DB, Persona DB, RabbitMQ）作为独立的 Deployment/StatefulSet
- 支持水平扩展（Horizontal Pod Autoscaling）

**状态**: ❌ 未实现

**相关文件**:
- 需要创建 `k8s/` 目录
- 需要创建 Deployment、Service、ConfigMap、Secret 等配置文件

**验收标准**:
- [ ] 创建所有组件的 Kubernetes 配置
- [ ] 支持水平扩展
- [ ] 支持配置管理（ConfigMap/Secret）
- [ ] 包含健康检查和就绪探针

**预计工期**: 1-2 周

---

### 9. Prometheus 指标集成 ⚠️

**来源**: `整体进度核对报告.md` - 下一步计划

**描述**: 
- 实现 Prometheus 指标收集
- 暴露 `/metrics` 端点
- 定义关键指标（响应时间、错误率、模型使用率等）

**状态**: ❌ 未实现

**相关文件**:
- 需要集成 `prometheus-client` 库
- 需要在 `src/main.py` 中添加指标收集

**验收标准**:
- [ ] 暴露 `/metrics` 端点
- [ ] 收集关键性能指标
- [ ] 收集业务指标（知识提取数量、用户画像数量等）
- [ ] 提供指标文档

**预计工期**: 3-5 天

---

### 10. 监控告警 ⚠️

**来源**: `整体进度核对报告.md` - 下一步计划

**描述**: 
- 配置监控告警规则
- 集成告警系统（如 Alertmanager）
- 定义告警阈值和通知渠道

**状态**: ❌ 未实现

**相关文件**:
- 需要创建告警规则配置文件
- 需要配置 Alertmanager

**验收标准**:
- [ ] 定义告警规则
- [ ] 配置告警通知（邮件、Slack 等）
- [ ] 测试告警触发
- [ ] 提供告警文档

**预计工期**: 3-5 天

---

### 11. 蓝绿部署实现 ⚠️

**来源**: `AAM (AI-Augmented Memory) SA v1.md` - 蓝绿部署章节

**描述**: 
- 实现蓝绿部署策略，实现零停机模型更新
- 支持模型版本切换
- 支持快速回滚

**状态**: ❌ 未实现

**相关文件**:
- 需要创建部署脚本或 CI/CD 配置
- 需要实现模型版本管理（部分已实现：`src/training/model_repository.py`）

**验收标准**:
- [ ] 实现蓝绿部署流程
- [ ] 支持模型版本切换
- [ ] 支持快速回滚
- [ ] 包含部署文档

**预计工期**: 1-2 周

---

## 🟢 低优先级（可选功能）

### 12. S3 存储支持（LoRA 训练）⚠️

**来源**: `AAM Phase II.md` - 批次五可选功能

**描述**: 
- 实现 S3 存储支持，用于生产环境模型存储
- 支持模型版本上传和下载

**状态**: ❌ 未实现

**相关文件**:
- `src/training/model_repository.py` - 当前只支持本地存储
- 需要扩展支持 S3

**验收标准**:
- [ ] 实现 S3 存储接口
- [ ] 支持模型版本上传
- [ ] 支持模型版本下载
- [ ] 支持配置 S3 凭证和桶名

**预计工期**: 3-5 天

---

### 13. vLLM Provider 完整实现 ⚠️

**来源**: `AAM Phase II.md` - 批次二（可选）

**描述**: 
- 完整实现 vLLM Provider（当前可能只有框架）
- 支持 OpenAI 兼容 API 调用

**状态**: ⚠️ 部分实现（需要确认）

**相关文件**:
- `src/infrastructure/ai/providers/vllm_provider.py` - 需要检查实现完整性

**验收标准**:
- [ ] vLLM Provider 完整实现
- [ ] 支持 OpenAI 兼容 API
- [ ] 包含单元测试
- [ ] 支持配置切换

**预计工期**: 3-5 天

---

### 14. KE 提取结果存储扩展 ⚠️

**来源**: `整体进度核对报告.md` - 下一步计划（低优先级）

**描述**: 
- 扩展 KE（知识提取）结果的存储方式
- 可能需要存储到独立的知识库或知识图谱

**状态**: ❌ 未实现

**相关文件**:
- `src/infrastructure/database/chroma_knowledge_store.py` - 当前 KE 结果存储在 ChromaDB
- 可能需要扩展存储结构

**验收标准**:
- [ ] 扩展 KE 结果存储结构
- [ ] 支持知识图谱存储（如果适用）
- [ ] 包含数据迁移脚本

**预计工期**: 1 周

---

## 📋 总结

### 按优先级分类

**高优先级（1 项）**:
1. 实际训练验证

**中优先级（8 项）**:
2. 预处理功能
3. LoRA 训练 CLI 命令
4. LoRA 训练任务调度
5. 批处理支持
6. 缓存机制
7. 性能监控
8. Kubernetes 配置
9. Prometheus 指标集成
10. 监控告警
11. 蓝绿部署实现

**低优先级（3 项）**:
12. S3 存储支持
13. vLLM Provider 完整实现
14. KE 提取结果存储扩展

### 预计总工期

- **高优先级**: 1-2 周
- **中优先级**: 8-12 周
- **低优先级**: 2-3 周
- **总计**: 约 11-17 周（取决于并行开发能力）

### 建议执行顺序

1. **第一阶段（2-3 周）**: 实际训练验证 + 预处理功能
2. **第二阶段（4-6 周）**: 性能优化（批处理、缓存、监控）
3. **第三阶段（4-6 周）**: 生产环境准备（Kubernetes、Prometheus、告警）
4. **第四阶段（2-3 周）**: 部署策略（蓝绿部署）
5. **第五阶段（2-3 周）**: 可选功能（CLI、调度、S3、vLLM）

---

**最后更新**: 2025-11-12  
**下次审查**: 完成高优先级任务后

