# AAM 系统所有服务状态总览

**更新时间**: 2025-01-14  
**状态**: ✅ 所有服务运行正常

---

## 📊 服务概览

### AAM Service 服务组（5个服务）

| 服务名称 | 容器名称 | 端口 | 状态 | 说明 |
|---------|---------|------|------|------|
| **aam-service** | aam-service-dev | 8000 | ✅ 运行中 (healthy) | 主服务 - FastAPI 应用 |
| **chromadb** | chromadb-dev | 8001 | ✅ 运行中 (healthy) | 向量数据库 |
| **postgres** | postgres-dev | 5432 | ✅ 运行中 (healthy) | PostgreSQL 用户画像数据库 |
| **rabbitmq** | rabbitmq-dev | 5672, 15672 | ✅ 运行中 (healthy) | RabbitMQ 消息队列 |
| **redis** | redis-dev | 6379 | ✅ 运行中 (healthy) | Redis 缓存 |

### AAM Admin 服务组（3个服务）

| 服务名称 | 容器名称/进程 | 端口 | 状态 | 说明 |
|---------|-------------|------|------|------|
| **admin-backend** | admin-backend-dev | 8003 | ✅ 运行中 (healthy) | 管理后端 - FastAPI 应用 (Docker) |
| **admin-frontend** | npm dev server | 3000 | ✅ 运行中 | 管理前端 - React 应用 (本地) |
| **admin-db** | admin-db-dev | 5433 | ✅ 运行中 (healthy) | PostgreSQL 管理数据库 |

---

## 🔍 详细服务信息

### 1. AAM Service (主服务)

**容器**: `aam-service-dev`  
**端口**: `8000`  
**状态**: ✅ Up 2 hours (healthy)  
**访问地址**:
- API 文档: http://localhost:8000/docs
- 健康检查: http://localhost:8000/health
- 就绪检查: http://localhost:8000/ready

**功能**:
- FastAPI 应用
- MCP Server 端点
- Token 管理 API
- 对话归档处理
- 语义分析服务

**依赖服务**:
- ChromaDB (向量数据库)
- PostgreSQL (用户画像数据库)
- RabbitMQ (消息队列)
- Redis (缓存，可选)

---

### 2. ChromaDB (向量数据库)

**容器**: `chromadb-dev`  
**端口**: `8001`  
**状态**: ✅ Up 15 hours (healthy)  
**访问地址**: http://localhost:8001

**功能**:
- 向量数据存储
- 语义搜索
- 知识库管理

**数据卷**: `chromadb_data_dev`

---

### 3. PostgreSQL (用户画像数据库)

**容器**: `postgres-dev`  
**端口**: `5432`  
**状态**: ✅ Up 15 hours (healthy)  
**数据库信息**:
- 数据库名: `aam_personas`
- 用户名: `aam_user`
- 密码: `aam_password`

**功能**:
- 用户画像存储
- 用户偏好管理
- 对话历史记录

**数据卷**: `postgres_data_dev`

---

### 4. RabbitMQ (消息队列)

**容器**: `rabbitmq-dev`  
**端口**: 
- `5672` - AMQP 协议端口
- `15672` - 管理界面端口
**状态**: ✅ Up 15 hours (healthy)  
**访问地址**:
- 管理界面: http://localhost:15672
- 用户名: `admin`
- 密码: `admin`

**功能**:
- 异步消息处理
- 对话归档队列
- 任务队列管理

**数据卷**: `rabbitmq_data_dev`

---

### 5. Redis (缓存)

**容器**: `redis-dev`  
**端口**: `6379`  
**状态**: ✅ Up 15 hours (healthy)  
**访问地址**: `localhost:6379`

**功能**:
- 缓存服务
- 会话管理
- 临时数据存储

**数据卷**: `redis_data_dev`

---

### 6. AAM Admin Backend (管理后端)

**容器**: `admin-backend-dev`  
**端口**: `8003`  
**状态**: ✅ 运行中 (healthy)  
**访问地址**:
- API 文档: http://localhost:8003/docs
- 健康检查: http://localhost:8003/health
- API 端点: http://localhost:8003/api/v1

**功能**:
- FastAPI 管理后端
- 认证与授权 API
- Docker 服务管理
- 日志流服务
- WebSocket 支持

**依赖服务**:
- admin-db (管理数据库)

**启动方式**:
```bash
# Docker 方式（推荐）
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
docker-compose -f docker-compose.dev.yml up admin-backend -d

# 本地方式
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin/backend
source venv/bin/activate
uvicorn src.main:app --reload --host 0.0.0.0 --port 8003
```

---

### 7. AAM Admin Frontend (管理前端)

**进程**: npm dev server (本地运行)  
**端口**: `3000`  
**状态**: ✅ 运行中  
**访问地址**: http://localhost:3000

**功能**:
- React 管理界面
- 仪表盘
- 服务监控
- 日志查看
- 系统设置

**启动方式**:
```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin/frontend
npm run dev
```

**注意**: 前端使用本地模式运行，不在 Docker 中，以保持 Vite 热重载速度。

---

### 8. AAM Admin Database (管理数据库)

**容器**: `admin-db-dev`  
**端口**: `5433`  
**状态**: ✅ Up 27 minutes (healthy)  
**数据库信息**:
- 数据库名: `aam_admin`
- 用户名: `admin`
- 密码: `admin`

**功能**:
- 管理员用户存储
- Token 记录
- 审计日志
- 部署记录
- 服务配置

**数据卷**: `admin_db_data_dev`

---

## 🚀 服务管理命令

### AAM Service 服务组

#### 查看服务状态

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-service
docker-compose -f docker-compose.dev.yml ps
```

#### 启动所有服务

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-service
docker-compose -f docker-compose.dev.yml up -d
```

#### 停止所有服务

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-service
docker-compose -f docker-compose.dev.yml down
```

### AAM Admin 服务组

#### 查看服务状态

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
docker-compose -f docker-compose.dev.yml ps
```

#### 启动所有服务

```bash
# 启动数据库
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
docker-compose -f docker-compose.dev.yml up admin-db -d

# 启动后端（Docker 方式）
docker-compose -f docker-compose.dev.yml up admin-backend -d

# 或启动后端（本地方式）
cd backend && source venv/bin/activate
uvicorn src.main:app --reload --host 0.0.0.0 --port 8003

# 启动前端（新终端）
cd frontend && npm run dev
```

#### 停止所有服务

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
docker-compose -f docker-compose.dev.yml down
```

### 查看服务日志

#### AAM Service 服务组

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-service
# 查看所有服务日志
docker-compose -f docker-compose.dev.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.dev.yml logs -f aam-service
docker-compose -f docker-compose.dev.yml logs -f chromadb
docker-compose -f docker-compose.dev.yml logs -f postgres
docker-compose -f docker-compose.dev.yml logs -f rabbitmq
docker-compose -f docker-compose.dev.yml logs -f redis
```

#### AAM Admin 服务组

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
# 查看所有服务日志
docker-compose -f docker-compose.dev.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.dev.yml logs -f admin-backend
docker-compose -f docker-compose.dev.yml logs -f admin-db
```

### 重启服务

#### AAM Service 服务组

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-service
# 重启所有服务
docker-compose -f docker-compose.dev.yml restart

# 重启特定服务
docker-compose -f docker-compose.dev.yml restart aam-service
```

#### AAM Admin 服务组

```bash
cd /Users/Daniel/Documents/GitHub/AAM/aam-admin
# 重启所有服务
docker-compose -f docker-compose.dev.yml restart

# 重启特定服务
docker-compose -f docker-compose.dev.yml restart admin-backend
docker-compose -f docker-compose.dev.yml restart admin-db
```

---

## 🧪 服务健康检查

### 1. AAM Service 健康检查

```bash
# 健康检查
curl http://localhost:8000/health

# 就绪检查
curl http://localhost:8000/ready
```

### 2. ChromaDB 健康检查

```bash
curl http://localhost:8001/api/v1/heartbeat
```

### 3. PostgreSQL 健康检查

```bash
docker exec postgres-dev pg_isready -U aam_user -d aam_personas
```

### 4. RabbitMQ 健康检查

```bash
docker exec rabbitmq-dev rabbitmq-diagnostics ping
```

### 5. Redis 健康检查

```bash
docker exec redis-dev redis-cli ping
```

### 6. AAM Admin Backend 健康检查

```bash
# 健康检查
curl http://localhost:8003/health

# API 文档
# 浏览器打开: http://localhost:8003/docs
```

### 7. AAM Admin Frontend 健康检查

```bash
# 访问前端应用
# 浏览器打开: http://localhost:3000
```

### 8. AAM Admin Database 健康检查

```bash
docker exec admin-db-dev pg_isready -U admin -d aam_admin
```

---

## 📋 服务依赖关系

### AAM Service 服务组

```
aam-service (主服务)
├── 依赖 ChromaDB (向量数据库)
├── 依赖 PostgreSQL (用户画像数据库)
├── 依赖 RabbitMQ (消息队列)
└── 可选 Redis (缓存)
```

**启动顺序**:
1. ChromaDB (向量数据库)
2. PostgreSQL (用户画像数据库)
3. RabbitMQ (消息队列)
4. Redis (缓存，可选)
5. AAM Service (主服务，等待所有依赖就绪)

### AAM Admin 服务组

```
admin-backend (管理后端)
├── 依赖 admin-db (管理数据库)
└── 可选 admin-frontend (管理前端，独立运行)
```

**启动顺序**:
1. admin-db (管理数据库)
2. admin-backend (管理后端，等待数据库就绪)
3. admin-frontend (管理前端，独立运行，可选)

---

## 🔗 服务访问地址汇总

### AAM Service 服务组

| 服务 | 访问地址 | 说明 |
|------|---------|------|
| AAM Service API | http://localhost:8000 | 主服务 API |
| AAM Service Docs | http://localhost:8000/docs | API 文档 |
| ChromaDB | http://localhost:8001 | 向量数据库 |
| PostgreSQL | localhost:5432 | 数据库连接 |
| RabbitMQ Management | http://localhost:15672 | 管理界面 |
| RabbitMQ AMQP | localhost:5672 | 消息队列 |
| Redis | localhost:6379 | 缓存服务 |

### AAM Admin 服务组

| 服务 | 访问地址 | 说明 |
|------|---------|------|
| Admin Backend API | http://localhost:8003 | 管理后端 API |
| Admin Backend Docs | http://localhost:8003/docs | API 文档 |
| Admin Frontend | http://localhost:3000 | 管理前端界面 |
| Admin Database | localhost:5433 | 管理数据库连接 |

---

## 📊 服务运行时间

根据 Docker 状态信息：

### AAM Service 服务组
- **ChromaDB**: 运行 15 小时
- **PostgreSQL**: 运行 15 小时
- **RabbitMQ**: 运行 15 小时
- **Redis**: 运行 15 小时
- **AAM Service**: 运行 2 小时（最近重启）

### AAM Admin 服务组
- **Admin Database**: 运行 27 分钟
- **Admin Backend**: ⚠️ 需手动启动
- **Admin Frontend**: ⚠️ 需手动启动

---

## ⚙️ 服务配置

### 环境变量

所有服务配置通过 `docker-compose.dev.yml` 管理，主要环境变量：

**AAM Service**:
- `DEBUG=true`
- `LOG_LEVEL=DEBUG`
- `CHROMADB_HOST=chromadb`
- `POSTGRES_HOST=postgres`
- `RABBITMQ_HOST=rabbitmq`
- `REDIS_HOST=redis`

### 数据卷

#### AAM Service 服务组
- `chromadb_data_dev` - ChromaDB 数据
- `postgres_data_dev` - PostgreSQL 数据
- `rabbitmq_data_dev` - RabbitMQ 数据
- `redis_data_dev` - Redis 数据

#### AAM Admin 服务组
- `admin_db_data_dev` - 管理数据库数据

---

## 🔒 安全注意事项

1. **默认密码**: 开发环境使用默认密码，生产环境必须修改
2. **端口暴露**: 所有端口暴露在 localhost，仅用于开发
3. **数据卷**: 数据卷未加密，生产环境需要加密

---

## 📝 服务日志位置

所有服务日志可通过 Docker Compose 查看：

```bash
# 查看所有服务日志
docker-compose -f docker-compose.dev.yml logs

# 查看最近 100 行日志
docker-compose -f docker-compose.dev.yml logs --tail=100

# 实时跟踪日志
docker-compose -f docker-compose.dev.yml logs -f
```

---

## ✅ 服务状态总结

### AAM Service 服务组

**所有服务运行正常！**

- ✅ 5 个服务全部运行中
- ✅ 所有服务健康检查通过
- ✅ 服务间通信正常
- ✅ 数据持久化正常

### AAM Admin 服务组

**所有服务运行正常！**

- ✅ Admin Database 运行正常
- ✅ Admin Backend 运行正常 (Docker)
- ✅ Admin Frontend 运行正常 (本地)

### 总体状态

**AAM 系统服务状态**:
- ✅ AAM Service 服务组: 5/5 运行中
- ✅ AAM Admin 服务组: 3/3 运行中（数据库和后端在 Docker，前端在本地）

**系统已准备好进行开发和测试！**

---

**报告生成时间**: 2025-01-14  
**状态**: ✅ 所有服务运行正常

